# 聚合设计规范

## 概述
聚合（Aggregate）是 DDD 中的一致性边界，确保数据变更的原子性和业务规则的一致性。本规范定义了聚合的设计原则和边界划分标准。

## 核心原则
1. 一致性边界：聚合内的数据必须满足最终一致性
2. 聚合根：每个聚合有且仅有一个聚合根实体
3. 小聚合优先：聚合应尽量小，减少事务范围
4. 通过 ID 引用：聚合之间通过 ID 而非对象引用

## 适用范围
适用于本项目中所有相关场景。
