# 事件追踪标准 — FAQ

## Q1: 事件和页面浏览的区别？
事件（Event）描述用户的主动行为（点击、滑动、提交）。页面浏览（PageView）描述页面访问。事件可以有属性（属性值），页面浏览通常只有路径和来源。

## Q2: 什么时候用客户端埋点 vs 服务端埋点？
客户端埋点：前端交互行为、UI 元素点击、页面浏览。服务端埋点：业务关键操作、下单支付、数据变更。关键业务事件建议双端埋点互相校验。
