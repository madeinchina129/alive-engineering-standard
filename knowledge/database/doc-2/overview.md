# 数据库迁移策略

## 概述
数据库迁移是软件系统演进中不可避免的操作，包括 Schema 变更、数据迁移和版本升级。本规范定义了迁移流程、回滚策略和零停机迁移方案。

## 核心原则
1. 先读后写：先确保新数据可读，再切换到新写入路径
2. 可回滚：每次迁移都必须有经过验证的回滚方案
3. 小步快跑：每次迁移变更小、风险低、易于隔离和排查
4. 数据校验：迁移前后必须对数据一致性进行全面校验

## 适用范围
适用于本项目中所有相关场景。
