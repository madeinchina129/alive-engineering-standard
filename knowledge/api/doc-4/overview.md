# API 文档规范

## 概述
良好的 API 文档是开发者体验的核心组成部分。本规范定义了 API 文档的内容标准、格式要求和发布流程，确保文档完整、准确、易于使用。

## 核心原则
1. 文档即代码：API 文档与代码一同维护、一同评审、一同发布
2. 入门友好：新开发者应在 5 分钟内完成第一个 API 调用
3. 示例驱动：每个 API 必须有请求/响应示例，覆盖常见场景
4. 持续更新：API 变更必须同步更新文档，文档评审是 CI 流程的一环

## 适用范围
适用于本项目中所有相关场景。
