# RESTful API 设计规范

## 概述
RESTful API 是微服务架构中最常见的服务间通信方式。本规范定义了 URI 设计、HTTP 方法使用、状态码选择、请求/响应格式和错误处理的标准。

## 核心原则
1. 资源导向：API 围绕资源而非动作设计
2. 自描述：每个请求和响应包含足够的上下文信息
3. 一致性：所有 API 遵循统一的命名、格式和行为约定
4. 版本兼容：向后兼容性优先，破坏性变更必须发新版本

## 适用范围
适用于本项目中所有相关场景。
