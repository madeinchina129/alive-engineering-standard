# GraphQL API 设计规范

## 概述
GraphQL 提供比 REST 更灵活的数据查询能力，客户端可以精确指定需要的数据字段。本规范定义了 Schema 设计、Query/Mutation 拆分、N+1 优化和权限控制的标准。

## 核心原则
1. Schema 驱动：API 行为由 Schema 定义，前后端基于 Schema 协作
2. 按需查询：客户端精确指定所需字段，避免数据过载
3. 批量化：使用 DataLoader 解决 N+1 查询问题
4. 安全第一：查询深度和复杂度限制是必需的

## 适用范围
适用于本项目中所有相关场景。
