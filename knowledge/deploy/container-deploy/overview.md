# 容器化部署规范

## 概述
容器化是现代化应用部署的标准方式。本规范定义了 Dockerfile 编写标准、镜像优化、安全扫描和容器运行时配置的最佳实践。

## 核心原则
1. 镜像精简：使用多阶段构建，最小化镜像体积
2. 单一职责：每个容器只运行一个进程
3. 不可变基础设施：容器视为一次性实体，不存储状态
4. 安全左移：在构建阶段就进行镜像安全扫描

## 适用范围
适用于本项目中所有相关场景。
